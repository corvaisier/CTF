# scan
- nmap
 => nmap -a <10.10.XX.XX>

# enumerate 
- enum4linux (peut trouver des fichiers cachés, utilisateurs et mots de passses)
 => enum4linux -a <10.10.XX.XX>

- gobuster (brute force des url)
 => gobuster dir -u <http://10.10.XX.XX> -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt 

# use vulnerability 
 - hydra (brute force des accès ssh si par ex on a un num d'utilisateur) 
 => hydra -l <pseudo> -P rockyou.txt <10.10.XX.XX> ssh

 - searchsploit (si une version a été trouvée, on put faire une recherche)
 => searchsploit <CMS Made simple | grep "2.x*">
	=> searchsploit -p <46635.py> || nano <66635.py> || search cve documentation
	=> python <path of file> --options
## when shell access => upgrade with python shell : 
 => python -c 'import pty; pty.spawn("/bin/bash")'

# escalade privilege
- gtfobins (site internet visant à escalader des privilèges sur unix) 